---
import Layout from "../../layouts/Layout.astro";
import { BLOG_POSTS } from "../../data/blog";

export function getStaticPaths() {
  return BLOG_POSTS.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;

const slugify = (s: string): string =>
  s
    .toLowerCase()
    .replace(/['"“”‘’]/g, "")
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/(^-|-$)/g, "");

// Render the lightweight markdown body into HTML blocks.
const renderBody = (body: string): { html: string; headings: { id: string; text: string }[] } => {
  const escape = (s: string) =>
    s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");

  const inline = (s: string) =>
    escape(s)
      .replace(
        /\[([^\]]+)\]\((https?:\/\/[^)]+)\)/g,
        '<a href="$2" class="font-bold text-indigo-600 underline underline-offset-2 hover:text-indigo-800 transition-colors">$1</a>'
      )
      .replace(/\*\*(.+?)\*\*/g, '<strong class="font-bold text-slate-900">$1</strong>')
      .replace(/\*(.+?)\*/g, '<em class="text-slate-800 font-medium">$1</em>');

  const li = (item: string) => {
    const text = item.replace(/^-\s*/, "");
    return `<li class="flex gap-3 items-start"><span class="mt-2.5 h-2 w-2 shrink-0 rounded-full bg-indigo-600 ring-4 ring-indigo-100"></span><span class="text-slate-700 leading-relaxed font-normal">${inline(
      text
    )}</span></li>`;
  };

  const parseTable = (tableBlock: string) => {
    const lines = tableBlock.trim().split("\n").filter((l) => l.trim().startsWith("|"));
    if (lines.length < 2) return "";
    const headers = lines[0]
      .split("|")
      .map((s) => s.trim())
      .filter((_, i, arr) => i > 0 && i < arr.length - 1);
    const rows = lines
      .slice(2)
      .map((line) =>
        line
          .split("|")
          .map((s) => s.trim())
          .filter((_, i, arr) => i > 0 && i < arr.length - 1)
      );

    const ths = headers
      .map((h) => `<th class="px-5 py-3.5 font-black text-slate-900">${inline(h)}</th>`)
      .join("");
    const trs = rows
      .map((row, idx) => {
        const tds = row
          .map((cell, cIdx) => {
            const isFirst = cIdx === 0;
            return `<td class="px-5 py-3.5 ${isFirst ? "font-bold text-slate-900" : "text-slate-700"}">${inline(cell)}</td>`;
          })
          .join("");
        const bg = idx % 2 === 0 ? "bg-white" : "bg-slate-50/70";
        return `<tr class="${bg} transition-colors hover:bg-indigo-50/40">${tds}</tr>`;
      })
      .join("");

    return `<div class="table-container my-8 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_8px_30px_rgba(0,0,0,0.06)]">
      <div class="overflow-x-auto">
        <table class="w-full min-w-[34rem] text-left text-sm">
          <thead>
            <tr class="border-b-2 border-indigo-100 bg-slate-50 text-xs font-black uppercase tracking-wider text-slate-700">
              ${ths}
            </tr>
          </thead>
          <tbody class="divide-y divide-slate-100">
            ${trs}
          </tbody>
        </table>
      </div>
    </div>`;
  };

  const out: string[] = [];
  const headings: { id: string; text: string }[] = [];
  let list: string[] = [];
  const flushList = () => {
    if (list.length) {
      out.push(`<ul class="my-6 space-y-3 pl-1">${list.join("")}</ul>`);
      list = [];
    }
  };

  body.split("\n\n").forEach((block) => {
    const trimmedBlock = block.trim();
    if (trimmedBlock.startsWith("|") && trimmedBlock.includes("\n|")) {
      flushList();
      out.push(parseTable(trimmedBlock));
      return;
    }

    const lines = block.split("\n");
    lines.forEach((line) => {
      const trimmed = line.trim();
      if (trimmed.startsWith("- ")) {
        list.push(li(trimmed));
        return;
      }
      flushList();

      if (trimmed.startsWith("### ")) {
        out.push(
          `<div class="mt-8 mb-4 rounded-xl border border-slate-200 bg-white p-5 shadow-sm hover:border-indigo-300 transition-colors"><h3 class="text-lg font-bold text-slate-900 sm:text-xl flex items-center gap-2.5"><span class="flex h-3 w-3 rounded-md bg-indigo-600"></span> ${inline(
            trimmed.replace("### ", "")
          )}</h3></div>`
        );
      } else if (trimmed.startsWith("## ")) {
        const text = trimmed.replace("## ", "");
        const id = slugify(text);
        headings.push({ id, text });
        out.push(
          `<h2 id="${id}" class="group mt-16 mb-5 scroll-mt-24 text-2xl font-black tracking-tight text-slate-900 sm:text-3xl border-l-4 border-indigo-600 pl-4 flex items-center justify-between"><a href="#${id}" class="hover:text-indigo-600 transition-colors">${inline(
            text
          )}</a><a href="#${id}" aria-label="Direct link to this section" class="text-sm font-bold text-indigo-600 opacity-0 transition-opacity group-hover:opacity-100">#</a></h2>`
        );
      } else if (trimmed.startsWith("Who should use it:")) {
        out.push(
          `<div class="my-5 rounded-xl border border-emerald-200 bg-emerald-50/70 p-4 text-sm text-emerald-950 shadow-sm"><strong class="font-bold text-emerald-800">Who should use it:</strong> ${inline(
            trimmed.replace(/^Who should use it:\s*/i, "")
          )}</div>`
        );
      } else if (trimmed.startsWith("Who should skip it:")) {
        out.push(
          `<div class="my-5 rounded-xl border border-amber-200 bg-amber-50/70 p-4 text-sm text-amber-950 shadow-sm"><strong class="font-bold text-amber-800">Who should skip it:</strong> ${inline(
            trimmed.replace(/^Who should skip it:\s*/i, "")
          )}</div>`
        );
      } else if (trimmed.startsWith("1. ") || trimmed.startsWith("2. ") || trimmed.startsWith("3. ") || trimmed.startsWith("4. ") || trimmed.startsWith("5. ")) {
        const num = trimmed.substring(0, 3);
        const rest = trimmed.substring(3);
        out.push(
          `<div class="my-4 rounded-xl border border-slate-200 bg-white p-4 text-sm text-slate-800 shadow-sm hover:border-indigo-200 transition-colors flex items-start gap-3"><span class="flex h-6 w-6 shrink-0 items-center justify-center rounded-lg bg-indigo-50 border border-indigo-200 text-xs font-black text-indigo-700">${num.replace('.', '').trim()}</span><div>${inline(rest)}</div></div>`
        );
      } else if (trimmed) {
        out.push(`<p class="mt-5 text-[1.05rem] leading-8 text-slate-700 font-normal">${inline(trimmed)}</p>`);
      }
    });
    flushList();
  });
  flushList();

  return { html: out.join(""), headings };
};

const { html: bodyHtml, headings } = renderBody(post.body);

// TL;DR is the first section of the body when present; the rest are article sections.
const hasTldr = headings.length > 0 && headings[0].text === "TL;DR";
const articleHeadings = hasTldr ? headings.slice(1) : headings;

// Split body: TL;DR summary vs rest.
let tldrContent = "";
let restHtml = bodyHtml;
if (hasTldr) {
  const h2Tag = bodyHtml.match(/<h2 id="tl-dr"[^>]*>.*?<\/h2>/);
  const tldrH2Start = bodyHtml.indexOf('<h2 id="tl-dr"');
  const contentStart = tldrH2Start + (h2Tag ? h2Tag[0].length : 0);
  const boundary = articleHeadings.length
    ? bodyHtml.indexOf(`<h2 id="${articleHeadings[0].id}"`)
    : bodyHtml.length;
  const rawTldr = bodyHtml.slice(contentStart, boundary);
  const plugIdx = rawTldr.indexOf("ollawrite.com");
  const cutIdx =
    plugIdx > -1 ? rawTldr.indexOf("</p>", plugIdx) + "</p>".length : rawTldr.length;
  tldrContent = rawTldr.slice(0, cutIdx);
  restHtml = rawTldr.slice(cutIdx) + bodyHtml.slice(boundary);
}

// Exactly 14 Article Chapters in the TOC Sidebar
const toc = articleHeadings;

const isAiContentGenerator = post.slug === "ai-content-generator-complete-guide";
const isBestAiWritingTools = post.slug === "best-ai-writing-tools-in-2026";
const isAiAgentsVsAssistants = post.slug === "ai-agents-vs-ai-assistants";

const showQuickPicks = isAiContentGenerator || isBestAiWritingTools || isAiAgentsVsAssistants;

const quickPickConfig = isAiContentGenerator
  ? {
      title: "The 5 AI Content Generator Architectures — Comparison Matrix",
      badge: "5 Architectures",
      headers: ["Generator Category", "Core Mechanism", "Best Fit", "Interactive Action"],
      rows: [
        {
          c1: "General Reasoning Models",
          c2: "Next-token prediction + Instruction tuning (Claude, ChatGPT)",
          c3: "Versatile essays, outlining, reasoning & drafts",
          actionUrl: "#the-main-types-of-ai-content-generators-youll-actually-run-into",
          actionText: "Open Chapter →",
        },
        {
          c1: "Marketing Workflow Platforms",
          c2: "Prompt templates + Multi-channel governance (Jasper, Copy.ai)",
          c3: "High-volume ad copy, emails & campaign assets",
          actionUrl: "#the-main-types-of-ai-content-generators-youll-actually-run-into",
          actionText: "Open Chapter →",
        },
        {
          c1: "SEO-Native Ranking Platforms",
          c2: "Live SERP scraping + Search intent scoring (Surfer, Frase)",
          c3: "Competitive search ranking & content briefs",
          actionUrl: "#the-main-types-of-ai-content-generators-youll-actually-run-into",
          actionText: "Open Chapter →",
        },
        {
          c1: "Niche Creative Specialists",
          c2: "Genre heuristics + Narrative continuity (Sudowrite)",
          c3: "Fiction manuscripts, plotting & creative scenes",
          actionUrl: "#the-main-types-of-ai-content-generators-youll-actually-run-into",
          actionText: "Open Chapter →",
        },
        {
          c1: "Site-Grounded Multi-Agent",
          c2: "Live sitemap crawling + Critic verification (OllaWrite)",
          c3: "Verifiable, factual, site-anchored publishing",
          actionUrl: "#the-main-types-of-ai-content-generators-youll-actually-run-into",
          actionText: "Open Chapter →",
        },
      ],
    }
  : isBestAiWritingTools
  ? {
      title: "Quick Pick — The Shortlist",
      badge: "8 Tools",
      headers: ["Tool", "Best for", "Category / Focus", "Interactive Action"],
      rows: [
        { c1: "ChatGPT", c2: "Versatile research, outlining & drafting", c3: "General Model", actionUrl: "#chatgpt-still-the-default", actionText: "Open →" },
        { c1: "Claude", c2: "Natural long-form flow & nuanced tone", c3: "General Model", actionUrl: "#claude-natural-long-form-writing", actionText: "Open →" },
        { c1: "Google Gemini", c2: "Google Workspace & Docs integration", c3: "Ecosystem", actionUrl: "#google-gemini-best-for-google-workspace", actionText: "Open →" },
        { c1: "Writesonic", c2: "High-volume SEO content at scale", c3: "SEO Drafting", actionUrl: "#writesonic-seo-focused-content", actionText: "Open →" },
        { c1: "Grammarly", c2: "Real-time grammar, clarity & polish", c3: "Editing", actionUrl: "#grammarly-best-for-editing-and-polish", actionText: "Open →" },
        { c1: "Surfer SEO", c2: "SERP-driven on-page ranking scoring", c3: "SEO Ranking", actionUrl: "#surfer-seo-for-search-rankings", actionText: "Open →" },
        { c1: "Notion AI", c2: "Workspace-integrated drafting & notes", c3: "Productivity", actionUrl: "#notion-ai-writing-inside-your-workspace", actionText: "Open →" },
        { c1: "Sudowrite", c2: "Fiction, plot brainstorming & scenes", c3: "Creative", actionUrl: "#sudowrite-for-fiction-writers", actionText: "Open →" },
      ],
    }
  : {
      title: "Architecture Matrix — The 4 Tiers",
      badge: "4 Tiers",
      headers: ["Architecture", "Best for", "Execution Model", "Interactive Action"],
      rows: [
        { c1: "AI Assistant", c2: "Conversational drafting, code review & legal redlines", c3: "Reactive / Chat", actionUrl: "#what-an-ai-content-writer-actually-is", actionText: "Open →" },
        { c1: "Contextual Assistant", c2: "Workspace document synthesis & file ingestion", c3: "Reactive / Workspace", actionUrl: "#what-an-ai-content-writer-actually-is", actionText: "Open →" },
        { c1: "Advisory Agent", c2: "24/7 background audit, pipeline & anomaly monitoring", c3: "Proactive / Scheduled", actionUrl: "#under-the-hood-the-5-step-pipeline", actionText: "Open →" },
        { c1: "Autonomous Agent", c2: "Multi-step tool execution with human approval gate", c3: "Proactive / Real Tools", actionUrl: "#under-the-hood-the-5-step-pipeline", actionText: "Open →" },
      ],
    };
---

<Layout title={`${post.title} — OllaWrite Journal`} description={post.excerpt}>
  <!-- Top Reading Progress Bar -->
  <div
    id="reading-progress-bar"
    class="fixed top-0 left-0 h-1.5 bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 z-50 transition-all duration-75 shadow-sm"
    style="width: 0%;"
  ></div>

  <!-- Toast notification for link copy -->
  <div
    id="copy-toast"
    class="fixed bottom-6 left-1/2 -translate-x-1/2 z-50 transform opacity-0 pointer-events-none transition-all duration-300 rounded-full border border-slate-800 bg-slate-900/95 backdrop-blur-md px-5 py-2.5 text-xs font-semibold text-white shadow-2xl flex items-center gap-2"
  >
    <span class="text-emerald-400 font-bold">✓</span> Copied link to clipboard
  </div>

  <article class="bg-white text-slate-900 min-h-screen">
    <!-- Top bar -->
    <div class="border-b border-slate-200 bg-white/90 backdrop-blur-md sticky top-16 z-30 shadow-sm">
      <div class="wrap flex items-center justify-between py-3.5">
        <a
          href="/blog"
          class="flex items-center gap-1.5 text-xs font-bold text-slate-600 hover:text-indigo-600 transition-colors"
        >
          ← Back to Journal
        </a>
        <a
          href="#table-of-contents-top"
          class="flex items-center gap-1.5 text-xs font-bold text-indigo-600 hover:text-indigo-800 transition-colors"
        >
          Skip to Table of Contents ↓
        </a>
      </div>
    </div>

    <!-- Article header -->
    <div class="border-b border-slate-200 bg-gradient-to-b from-slate-50 via-indigo-50/20 to-white">
      <div class="wrap py-12 lg:py-16">
        <div class="flex flex-wrap items-center gap-3 text-xs font-bold uppercase tracking-widest text-indigo-600">
          <span class="rounded-full border border-indigo-200 bg-indigo-50 px-3.5 py-1 text-indigo-700 shadow-sm">
            {post.category}
          </span>
          <span class="text-slate-300">•</span>
          <span class="text-slate-500 font-semibold">{post.date}</span>
          <span class="text-slate-300">•</span>
          <span class="text-slate-500 font-semibold">{post.readTime}</span>
        </div>
        <h1
          class="mt-5 max-w-4xl text-3xl font-black leading-[1.12] tracking-tight sm:text-4xl lg:text-[3.25rem] text-slate-900"
        >
          {post.title}
        </h1>
        <p class="mt-5 max-w-3xl text-lg leading-relaxed text-slate-600 font-normal">
          {post.excerpt}
        </p>
        <div class="mt-6 flex items-center gap-3 text-sm text-slate-500">
          <span class="grid h-10 w-10 place-items-center rounded-full bg-gradient-to-tr from-indigo-600 to-purple-600 text-sm font-black text-white shadow-md">OW</span>
          <div>
            <p class="font-bold text-slate-900">{post.author.name}</p>
            <p class="text-xs text-slate-500 font-medium">{post.author.role}</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Body: sticky TOC sidebar + full-width content -->
    <div class="wrap py-12 lg:py-16">
      <div class="lg:flex lg:gap-10">
        <!-- VIBRANT HIGH-CONTRAST TOC SIDEBAR WITH 14 CHAPTERS -->
        <aside class="mb-10 lg:mb-0 lg:w-76 lg:shrink-0">
          <nav class="rounded-2xl border-2 border-indigo-600/20 bg-white p-4 shadow-[0_12px_36px_rgba(79,70,229,0.08)] lg:sticky lg:top-28">
            <!-- Sidebar Header Banner -->
            <div class="flex items-center justify-between rounded-xl bg-slate-900 px-3.5 py-2.5 text-white shadow-sm">
              <h2 class="flex items-center gap-2 text-xs font-black uppercase tracking-wider text-white">
                <span class="text-indigo-400">☰</span> Table of Contents
              </h2>
              <span class="rounded-full bg-indigo-600 px-2.5 py-0.5 text-[0.65rem] font-black text-white shadow-sm">
                {toc.length} Chapters
              </span>
            </div>

            <!-- Quick jump pill inside sidebar for TL;DR -->
            {
              hasTldr && (
                <div class="mt-3 border-b border-slate-100 pb-2.5">
                  <a
                    href="#tl-dr"
                    class="group flex items-center justify-between rounded-xl bg-gradient-to-r from-indigo-50 to-purple-50 border border-indigo-200/80 px-3 py-2 text-xs font-bold text-indigo-700 transition-all hover:border-indigo-400 hover:from-indigo-100 hover:to-purple-100 shadow-sm"
                  >
                    <span class="flex items-center gap-1.5">
                      <span class="text-indigo-600">⚡</span> 30-Sec TL;DR
                    </span>
                    <span class="text-[0.65rem] font-bold text-indigo-500 group-hover:text-indigo-800">Jump ↓</span>
                  </a>
                </div>
              )
            }

            <ol id="sidebar-toc" class="mt-3 space-y-1.5 max-h-[62vh] overflow-y-auto pr-1 toc-scrollbar">
              {
                toc.map((h, i) => (
                  <li>
                    <a
                      href={`#${h.id}`}
                      data-target={h.id}
                      class="toc-sidebar-link group flex items-center gap-2.5 rounded-xl border border-slate-200 bg-white px-2.5 py-2 text-xs font-bold text-slate-800 transition-all hover:border-indigo-500 hover:bg-indigo-50/50 hover:text-indigo-700 hover:shadow-sm"
                    >
                      <span class="toc-badge flex h-5 w-5 shrink-0 items-center justify-center rounded-lg bg-indigo-600 text-[0.65rem] font-black text-white shadow-sm transition-colors group-hover:bg-indigo-700">
                        {String(i + 1).padStart(2, "0")}
                      </span>
                      <span class="flex-1 truncate leading-tight font-semibold">{h.text}</span>
                      <span class="text-slate-300 transition-transform group-hover:translate-x-0.5 group-hover:text-indigo-600 text-[0.7rem] font-bold">→</span>
                    </a>
                  </li>
                ))
              }
            </ol>
          </nav>
        </aside>

        <!-- Article content -->
        <div class="min-w-0 flex-1 lg:max-w-[56rem]">
          <!-- 1. TABLE OF CONTENTS: CHAPTER MODULES (TOP) -->
          <section id="table-of-contents-top" class="mb-8 scroll-mt-32">
            <!-- Modern High-Contrast White Chapter Grid with Vibrant Badges -->
            <div class="overflow-hidden rounded-2xl border-2 border-indigo-600/20 bg-white p-6 shadow-[0_12px_36px_rgba(79,70,229,0.06)]">
              <div class="flex items-center justify-between border-b border-slate-100 pb-4">
                <div class="flex items-center gap-2.5">
                  <span class="grid h-7 w-7 place-items-center rounded-lg bg-indigo-600 text-xs font-black text-white shadow-sm">☰</span>
                  <h3 class="text-sm font-black uppercase tracking-wider text-slate-900">
                    Table of Contents — Chapter Modules
                  </h3>
                </div>
                <span class="rounded-full bg-indigo-50 border border-indigo-200 px-3 py-1 text-xs font-black text-indigo-700 shadow-sm">
                  {articleHeadings.length} Chapters
                </span>
              </div>
              <div class="mt-5 grid grid-cols-1 gap-3.5 sm:grid-cols-2 lg:grid-cols-3">
                {
                  articleHeadings.map((h, i) => (
                    <a
                      href={`#${h.id}`}
                      class="group relative flex flex-col justify-between rounded-xl border border-slate-200 bg-white p-4 shadow-sm transition-all duration-200 hover:-translate-y-1 hover:border-indigo-500 hover:bg-indigo-50/30 hover:shadow-md"
                    >
                      <div class="flex items-center justify-between">
                        <span class="flex h-6 w-6 items-center justify-center rounded-lg bg-indigo-600 text-xs font-black text-white shadow-sm transition-transform group-hover:scale-105">
                          {String(i + 1).padStart(2, "0")}
                        </span>
                        <span class="text-xs font-bold text-slate-400 transition-colors group-hover:text-indigo-600">
                          Open →
                        </span>
                      </div>
                      <span class="mt-3 text-xs font-bold text-slate-900 leading-snug transition-colors group-hover:text-indigo-600">
                        {h.text}
                      </span>
                    </a>
                  ))
                }
              </div>
            </div>
          </section>

          <!-- 2. TL;DR SUMMARY (BEFORE THE MATRIX TABLE) -->
          {
            hasTldr && (
              <section id="tl-dr" class="relative mb-8 scroll-mt-32 overflow-hidden rounded-2xl border-2 border-indigo-950 bg-gradient-to-br from-slate-950 via-indigo-950 to-slate-900 text-white shadow-[0_16px_40px_rgba(15,23,42,0.25)]">
                <div class="h-1.5 bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500"></div>
                <div class="p-6 sm:p-8">
                  <div class="flex flex-wrap items-center gap-3">
                    <span class="rounded-lg border border-indigo-400/40 bg-indigo-500/30 px-3 py-1 text-xs font-black uppercase tracking-wider text-indigo-300 shadow-sm">
                      TL;DR
                    </span>
                    <span class="text-xs font-bold uppercase tracking-wider text-indigo-200">
                      Too long? Here's the whole article in 30 seconds
                    </span>
                  </div>
                  <div class="mt-4 border-l-4 border-indigo-400 pl-5 tldr-body" set:html={tldrContent} />
                </div>
              </section>
            )
          }

          <!-- 3. COMPARISON MATRIX TABLE (AFTER TL;DR) -->
          {
            showQuickPicks && (
              <section class="mb-10 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_10px_30px_rgba(0,0,0,0.06)]">
                <div class="flex items-center justify-between bg-slate-900 px-6 py-4">
                  <h3 class="text-sm font-black uppercase tracking-wider text-indigo-300 flex items-center gap-2">
                    <span class="text-amber-400">⚡</span> {quickPickConfig.title}
                  </h3>
                  <span class="rounded-full bg-slate-800 border border-slate-700 px-2.5 py-0.5 text-[0.65rem] font-bold uppercase tracking-wider text-slate-300">
                    {quickPickConfig.badge}
                  </span>
                </div>
                <div class="overflow-x-auto">
                  <table class="w-full min-w-[34rem] text-left text-sm">
                    <thead>
                      <tr class="border-b border-slate-200 bg-slate-50 text-xs font-bold uppercase tracking-wider text-slate-600">
                        <th class="px-6 py-3.5">{quickPickConfig.headers[0]}</th>
                        <th class="px-6 py-3.5">{quickPickConfig.headers[1]}</th>
                        <th class="px-6 py-3.5">{quickPickConfig.headers[2]}</th>
                        <th class="px-6 py-3.5 text-right">{quickPickConfig.headers[3]}</th>
                      </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100">
                      {
                        quickPickConfig.rows.map((row, i) => (
                          <tr class={`transition-colors hover:bg-indigo-50/40 ${i % 2 === 0 ? "bg-white" : "bg-slate-50/50"}`}>
                            <td class="px-6 py-4 font-black text-slate-900">{row.c1}</td>
                            <td class="px-6 py-4 text-slate-600 text-xs leading-relaxed">{row.c2}</td>
                            <td class="px-6 py-4 font-bold text-indigo-600 text-xs">{row.c3}</td>
                            <td class="px-6 py-4 text-right">
                              <a
                                href={row.actionUrl}
                                class="inline-flex items-center gap-1 rounded-lg border border-indigo-200 bg-indigo-50 px-3 py-1.5 text-xs font-black text-indigo-700 transition-all hover:bg-indigo-600 hover:text-white hover:border-indigo-600 shadow-sm"
                              >
                                {row.actionText}
                              </a>
                            </td>
                          </tr>
                        ))
                      }
                    </tbody>
                  </table>
                </div>
              </section>
            )
          }

          <!-- 4. REMAINING CONTENT DIRECTLY -->
          <div set:html={restHtml} />

          <!-- Bottom quick action bar: direct link to TOC & TL;DR -->
          <div class="mt-12 flex flex-wrap items-center justify-between gap-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_8px_30px_rgba(0,0,0,0.04)]">
            <div class="flex items-center gap-3 text-sm font-semibold text-slate-700">
              <span class="grid h-7 w-7 place-items-center rounded-full bg-indigo-50 border border-indigo-200 text-xs font-bold text-indigo-600">⚡</span>
              <span>Finished reading? Need to review chapters or summary?</span>
            </div>
            <div class="flex items-center gap-3">
              <a
                href="#table-of-contents-top"
                class="inline-flex items-center gap-1.5 rounded-xl border border-slate-200 bg-slate-50 px-4 py-2 text-xs font-bold text-slate-700 transition-all hover:border-indigo-400 hover:text-indigo-700 hover:bg-white shadow-sm"
              >
                ↑ Table of Contents
              </a>
              {
                hasTldr && (
                  <a
                    href="#tl-dr"
                    class="inline-flex items-center gap-1.5 rounded-xl border border-indigo-600 bg-indigo-600 px-4 py-2 text-xs font-bold uppercase tracking-wider text-white shadow-sm transition-all hover:bg-indigo-700 hover:shadow-md"
                  >
                    ↑ 30-sec TL;DR
                  </a>
                )
              }
              <a
                href="https://login.ollawrite.com"
                class="inline-flex items-center gap-1.5 rounded-xl border border-slate-900 bg-slate-900 px-4 py-2 text-xs font-bold uppercase tracking-wider text-white transition-all hover:bg-slate-800 shadow-sm"
              >
                Audit your site free →
              </a>
            </div>
          </div>

          <!-- Floating Action Control Pill (White contrast aesthetic) -->
          <div class="fixed bottom-6 right-6 z-40 flex items-center gap-2 rounded-full border border-slate-300 bg-white/95 p-1.5 shadow-[0_12px_36px_rgba(0,0,0,0.12)] backdrop-blur-md">
            <a
              href="#main"
              title="Back to Top"
              class="flex h-9 w-9 items-center justify-center rounded-full bg-slate-100 text-xs font-black text-slate-700 transition-colors hover:bg-indigo-600 hover:text-white"
            >
              ↑
            </a>
            {
              hasTldr && (
                <a
                  href="#tl-dr"
                  title="Jump to TL;DR"
                  class="flex h-9 px-3.5 items-center justify-center rounded-full bg-indigo-50 border border-indigo-200 text-xs font-black text-indigo-700 transition-colors hover:bg-indigo-600 hover:text-white"
                >
                  ⚡ TL;DR
                </a>
              )
            }
            <button
              id="copy-article-link"
              type="button"
              title="Copy link to clipboard"
              class="flex h-9 w-9 items-center justify-center rounded-full bg-slate-100 text-xs font-bold text-slate-700 transition-colors hover:bg-indigo-600 hover:text-white"
            >
              🔗
            </button>
          </div>

          <!-- Email signup (Clean pleasant contrast) -->
          <div class="mt-10 overflow-hidden rounded-2xl border border-slate-900/10 bg-gradient-to-br from-slate-950 via-indigo-950 to-slate-900 p-6 text-white shadow-xl">
            <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
              <div>
                <p class="text-[0.7rem] font-bold uppercase tracking-widest text-indigo-300">
                  The OllaWrite Journal
                </p>
                <h3 class="mt-1 text-base font-bold text-white sm:text-lg">
                  Get weekly AI writing deep-dives in your inbox
                </h3>
              </div>
              <form
                action="https://login.ollawrite.com/register"
                method="get"
                class="flex items-center gap-2"
              >
                <label for="journal-email" class="sr-only">Email address</label>
                <input
                  id="journal-email"
                  type="email"
                  name="email"
                  required
                  placeholder="you@company.com"
                  class="w-48 sm:w-56 rounded-xl border border-white/20 bg-white/10 px-3.5 py-2.5 text-xs text-white placeholder-indigo-200/70 outline-none focus:border-indigo-400 focus:ring-1 focus:ring-indigo-400 backdrop-blur-sm"
                />
                <button
                  type="submit"
                  class="whitespace-nowrap rounded-xl bg-indigo-600 px-4 py-2.5 text-xs font-bold text-white transition-all hover:bg-indigo-500 shadow-sm"
                >
                  Sign up →
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </article>

  <!-- Script for Reading Progress Bar, Scroll Spy, & Copy Actions -->
  <script>
    // 1. Reading progress bar
    const progressBar = document.getElementById("reading-progress-bar");
    const updateProgress = () => {
      const scrollTotal = document.documentElement.scrollHeight - window.innerHeight;
      if (scrollTotal > 0 && progressBar) {
        const scrolled = (window.scrollY / scrollTotal) * 100;
        progressBar.style.width = Math.min(100, Math.max(0, scrolled)) + "%";
      }
    };
    window.addEventListener("scroll", updateProgress, { passive: true });
    updateProgress();

    // 2. Active Sidebar TOC Spy (14 Chapters)
    const headings = document.querySelectorAll("article h2[id]");
    const tocLinks = document.querySelectorAll(".toc-sidebar-link");

    const highlightToc = () => {
      let currentId = "";
      const scrollPos = window.scrollY + 160;

      headings.forEach((heading) => {
        const top = (heading as HTMLElement).offsetTop;
        if (scrollPos >= top) {
          currentId = heading.id;
        }
      });

      tocLinks.forEach((link) => {
        const target = link.getAttribute("data-target");
        const badge = link.querySelector(".toc-badge");
        if (target === currentId) {
          link.classList.add("border-indigo-600", "bg-indigo-50/80", "text-indigo-900", "font-bold", "shadow-sm");
          link.classList.remove("border-slate-200", "bg-white", "text-slate-800");
          badge?.classList.add("bg-indigo-600", "text-white");
          badge?.classList.remove("bg-slate-100", "text-slate-700");
        } else {
          link.classList.remove("border-indigo-600", "bg-indigo-50/80", "text-indigo-900", "font-bold", "shadow-sm");
          link.classList.add("border-slate-200", "bg-white", "text-slate-800");
          badge?.classList.add("bg-indigo-600", "text-white");
        }
      });
    };
    window.addEventListener("scroll", highlightToc, { passive: true });
    highlightToc();

    // 3. One-Click Copy Article URL Toast
    const copyBtn = document.getElementById("copy-article-link");
    const toast = document.getElementById("copy-toast");
    copyBtn?.addEventListener("click", () => {
      navigator.clipboard.writeText(window.location.href).then(() => {
        if (toast) {
          toast.classList.remove("opacity-0", "pointer-events-none");
          toast.classList.add("opacity-100");
          setTimeout(() => {
            toast.classList.remove("opacity-100");
            toast.classList.add("opacity-0", "pointer-events-none");
          }, 2500);
        }
      });
    });
  </script>
</Layout>

<style is:global>
  /* Custom pleasant slim scrollbar for TOC sidebar */
  .toc-scrollbar::-webkit-scrollbar {
    width: 4px;
  }
  .toc-scrollbar::-webkit-scrollbar-track {
    background: transparent;
  }
  .toc-scrollbar::-webkit-scrollbar-thumb {
    background: #cbd5e1;
    border-radius: 9999px;
  }
  .toc-scrollbar::-webkit-scrollbar-thumb:hover {
    background: #94a3b8;
  }

  /* High contrast text inside the TL;DR box */
  .tldr-body,
  .tldr-body * {
    color: #ffffff !important;
  }
  .tldr-body p {
    margin-top: 0.75rem;
    font-size: 1.05rem;
    line-height: 1.8;
    font-weight: 400;
    color: #f1f5f9 !important;
  }
  .tldr-body strong {
    color: #a5b4fc !important;
    font-weight: 700;
  }
  .tldr-body a {
    color: #818cf8 !important;
    font-weight: 700;
    text-decoration: underline;
    text-underline-offset: 3px;
  }
  .tldr-body ul {
    margin: 1rem 0;
    display: grid;
    gap: 0.75rem;
  }
  .tldr-body li span {
    color: #f8fafc !important;
    font-weight: 400;
  }
</style>
